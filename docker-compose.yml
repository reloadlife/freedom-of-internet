version: '3.7'

services:
  openvpn:
    container_name: "openvpn-server"
    build:
      context: ./openvpn
      dockerfile: Dockerfile
    env_file:
        - .env
    volumes:
      - ./openvpn/data/:/etc/openvpn
    restart: always
    ports:
      - "${OPENVPN_PORT}:1194/udp"